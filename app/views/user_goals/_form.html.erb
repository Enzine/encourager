<%= form_for(user_goal) do |f| %>
  <% if user_goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_goal.errors.count, "error") %> prohibited your goal from being saved:</h2>

      <ul>
      <% user_goal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    Rate your performance (1-5 stars).
    <%= f.label :stars %>
    <%= f.number_field :stars, in: 1..5 %>

    Comment on your performance or related feelings.
    <%= f.label :comments %>
    <%= f.text_field :comments %>

    <%= f.submit "Update goal", class: 'button' %>
<% end %>
