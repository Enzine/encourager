<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Encourager" %></title>

    <link href="https://fonts.googleapis.com/css?family=Signika:300,400,600,700" rel="stylesheet2">
    <link href="https://fonts.googleapis.com/css?family=Baloo" rel="stylesheet">
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <style>
    textarea{width:50%}
    input[type=text] {
        width: 50%;
    }

    input[type=password] {
        width: 50%;
    }

    input[type=number] {
      width: 10%;
    }
  </style>
  <body>
    <div class="top-bar" id="navigation-menu">
    	<div class="top-bar-right">
    		<ul class="menu">
          <% if current_user.nil? %>
    			  <li class="menu-text"><%= link_to 'Register', new_user_path %></li>
        	  <li class="menu-text"><%= link_to 'Login', login_path %></li>
          <% else %>
            <li class="menu-text"><%= link_to current_user.username, current_user %></li>
            <li class="menu-text"><%= link_to 'Logout', logout_path, method: :delete %></li>
          <% end %>
    		</ul>
    	</div>
    	<div class="top-bar-left">
    		<ul class="menu">
    			<li class="menu-text"><%= link_to 'Start page', root_path %></li>
    			<li class="menu-text"><%= link_to 'Users', users_path %></li>
          <li class="menu-text"><%= link_to 'Goals', goals_path %></li>
    		</ul>
    	</div>
    </div>

    <%= yield %>

    <%= javascript_include_tag "application" %>

  </body>
</html>
